<Window x:Class="The_Movies.ScreeningsOverview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:The_Movies"
        mc:Ignorable="d"
        Title="ScreeningsOverview" Height="450" Width="800">
    <Grid Margin="50,95,50,111">
        <DataGrid ItemsSource="{Binding Showings}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Film" Binding="{Binding MovieTitle}" Width="150"/>
                <DataGridTextColumn Header="biograf" Binding="{Binding CinemaName}" Width="150" />
                <DataGridTextColumn Header="Sal" Binding="{Binding Auditorium}" />
                <DataGridTextColumn Header="Dato" Binding="{Binding Date}" />
                <DataGridTextColumn Header="tid" Binding="{Binding TimeRange}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
