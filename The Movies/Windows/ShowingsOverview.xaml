<Window x:Class="The_Movies.ShowingsOverview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:The_Movies"
        mc:Ignorable="d"
        Title="ScreeningsOverview" Height="450" Width="800">
    <Grid Margin="50,95,50,111">
        <DataGrid ItemsSource="{Binding Showings}" AutoGenerateColumns="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Film" Binding="{Binding MovieTitle}" Width="3*"/>
                <DataGridTextColumn Header="biograf" Binding="{Binding CinemaName}" Width="3*" />
                <DataGridTextColumn Header="Sal" Binding="{Binding TheaterName}" />
                <DataGridTextColumn Header="Dato" Binding="{Binding StartDate, StringFormat=dd-MM-yyyy}" />
                <DataGridTextColumn Header="Starttid" Binding="{Binding StartDate, StringFormat=HH:mm}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
